## 1. Implementation

- [ ] 1.1 Define supported URI schemes and dataset `base_uri` resolution rules
- [ ] 1.2 Implement a filesystem abstraction using `fsspec` or equivalent
- [ ] 1.3 Implement UUID v7-based file naming for Parquet outputs
- [ ] 1.4 Integrate storage and naming into the write path (without changing higher-level semantics)
- [ ] 1.5 Add tests for local and remote backends (e.g., file://, s3://) and naming behavior

